module Recursion;

type Nil : * = product { };
type Cons : * = product { head: i32, tail: List };
type List : * = sum { nil: Nil, cons: Cons };

type List2 : * = sum {
  nil: product { },
  cons: product { head: i32, tail: List2 },
};

module TDLOutput;

type Zero : *;
type Zero = sum { };

type One : *;
type One = product { };

type Maybe : *;
type Maybe = sum { just: i32, nothing: One };

/* comment */

type EmptyArray = array sum { };

module Product;

type UserID : *;
type UserID = text;

type User : *;
type User = product {
  id: UserID,
  age: i32,
};

type Singleton1 : *;
type Singleton1 = product { id: UserID };

type Singleton2 : *;
type Singleton2 = sum { id: UserID };
